// sphere.h - Fichier d'en-tête contenant les déclarations de fonctions pour la sphère

// Inclusion de la bibliothèque mathématique pour utiliser la constante M_PI
#include <math.h>

// Fonction pour calculer la surface de la sphère
float calculer_surface(float rayon);

// Fonction pour calculer le volume de la sphère
float calculer_volume(float rayon);
